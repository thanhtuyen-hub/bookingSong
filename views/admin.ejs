<!DOCTYPE html>
<html lang="vi">
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Quản trị Danh sách</title>
  <style>
    body { 
        font-family: sans-serif; 
        margin: 10px auto;
        background-image: url('atoan.jpg'); /* Đường dẫn đến hình ảnh nền */

    }

    .container {
        max-width: 990px;
        margin: 25px auto;
        background-color: #fff;
        padding: 30px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        border-radius: 10px;
    }

    h2 { 
        text-align: center; 
        font-size: 2.5rem;
    }

    table {
        width: 100%;
        border-collapse: collapse;
    }

    table, th, td {
        border: 1px solid #ccc;
    }

    th, td {
        padding: 10px;
        text-align: left;
        font-size: 1.5rem;
    }

    tr > td:nth-child(5)
    {
        text-align: center;
    }

    th {
        background-color: #f4f4f4;
    }

    .table-wrapper {
        max-height: 600px;
        overflow-y: auto;
        border: 1px solid #ccc; 
    }

    .actions button {
        margin: 5px;
        padding: 5px 10px;
        font-size: 1rem;
        cursor: pointer;
    }

    .link{
      display: flex;
      justify-content: space-between;
    }

    button.delete-all {
      background-color: #e53935;
      color: white;
      padding: 8px 16px;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      font-weight: bold;
    }

    button.delete-all:hover {
      background-color: #c62828;
    }


    .actions button.edit { background-color: #ff9800; color: white; }
    .actions button.delete { background-color: #f44336; color: white; }

    @media screen and (max-width: 768px) {
        .container{
          padding: 30px 10px;
          margin: 0 10px;
        }

        th, td{
            font-size: 1rem;
        }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="link">
      <a href="/" class="home">Trang Chủ</a>
      <button class="delete-all" onclick="if(confirm('Bạn có chắc muốn xóa toàn bộ không?')) location.href='/admin/delete-all'">
        🗑️ Xóa tất cả
      </button>
    </div>
    <h2>💥 Quản trị Danh sách Đăng ký 💥</h2>

    <div class="table-wrapper">
        <table>
            <thead>
              <tr>
                <th>STT</th>
                <th>Tên</th>
                <th>Bài hát</th>
                <th>Thời gian đăng ký</th>
                <th>Hành động</th>
              </tr>
            </thead>
            <tbody>
              <% data.forEach((entry, index) => { %>
              <tr>
                <td><%= index + 1 %></td>
                <td><%= entry.name %></td>
                <td><%= entry.song %></td>
                <td><%= new Date(entry.timestamp).toLocaleString() %></td>
                <td class="actions">
                  <button class="delete" onclick="location.href='/admin/delete-by-time/<%= entry.timestamp %>'">Xóa</button>
                </td>
              </tr>
              <% }) %>
            </tbody>
          </table>
    </div>
  </div>
</body>
</html>
